import 'package:flutter/material.dart';

class ErrorContentsWidget extends StatelessWidget {
  const ErrorContentsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('エラーが発生しました。'),
    );
  }
}